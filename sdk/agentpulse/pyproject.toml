[project]
name = "agentpulse-astrik"
version = "0.1.0"
description = "Business intelligence observability layer for agentic AI systems"
requires-python = ">=3.9"
dependencies = ["httpx>=0.25.0"]

[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"